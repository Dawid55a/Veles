<Application x:Class="Veles_Application.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Veles_Application"
             xmlns:views="clr-namespace:Veles_Application.Views"
             xmlns:viewmodels="clr-namespace:Veles_Application.ViewModels"
             Startup="Main">
    <Application.Resources>
        <DataTemplate DataType="{x:Type viewmodels:GroupViewModel}">
            <views:GroupView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type viewmodels:ChatViewModel}">
            <views:ChatView/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type viewmodels:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:SearchGroupViewModel}">
            <views:SearchGroupView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:CreateGroupViewModel}">
            <views:CreateGroupView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:UsersViewModel}">
            <views:UsersView/>
        </DataTemplate>
    </Application.Resources>
</Application>
